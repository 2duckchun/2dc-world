import type { GetCapabilitiesOutput } from "./schema"

export const getCapabilitiesFixture: GetCapabilitiesOutput = {
  role: "anonymous",
  canInteract: false,
  canLike: false,
  canComment: false,
  canReply: false,
  canWrite: false,
  canEdit: false,
  canPublish: false,
  canDeleteComment: false,
}
